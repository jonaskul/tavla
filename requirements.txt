fastapi>=0.111.0
sqlmodel>=0.0.19
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
boto3>=1.34.0
weasyprint>=62.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
alembic>=1.13.0
python-dotenv>=1.0.0
# PostgreSQL driver. SQLite needs no driver and stays the local default, so
# this is only exercised when DATABASE_URL points at PostgreSQL.
psycopg[binary]>=3.1.0
# Validates the address on the sign-in endpoints.
pydantic[email]>=2.0.0
# HTTP client for the Resend API.
httpx>=0.27.0
